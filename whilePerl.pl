#!/usr/bin/perl
use strict;
use warnings;
my $contador = 1;
while ($contador <= 5) {
    print "Contador: $contador\n";
    $contador++;
}
# Con do..while.
# do {
#     print "Contador: $contador\n";
#     $contador ++;
# } while ($contador <= 5);